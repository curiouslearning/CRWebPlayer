// TextElement type that describes the text element model

export type TextElement = {
    positionX: number;
    positionY: number;
    width: number;
    height: number;
    textContentAsHTML: string;
}