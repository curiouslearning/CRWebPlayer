// WordTimestampElements class that contains all the timestamp audio objects for the page

import { WordTimestampElement } from "./WordTimestampElement";

export type AudioTimestamps = {
    timestamps: WordTimestampElement[];
}

